#include "main.h"

/**
 * int _islower(int c) - write a function that checks for lowercase
 * Return;1 if c is lowercase
 * else 0
 * @c; The character in ASCII code
 */
int _islower(int c)
{
	if (c >= 97 && c <= 122)
	{
		return (0);
	}
	else
	{
		return (0);
	}
	_putchar('\n');
}

